const metricValidators = require('./metricValidators');
const authValidators = require('./authValidators');
const goalValidators = require('./goalValidators');
const groupValidators = require('./groupValidators');
module.exports = {
    metricValidators,
    authValidators,
    goalValidators,
    groupValidators
};