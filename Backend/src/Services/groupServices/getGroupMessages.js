const getGroupMessagesService = (groupId)=>{
    // this function will get all the messages of a group
    // It will return an array of message objects
    return
};

module.exports =  getGroupMessagesService;