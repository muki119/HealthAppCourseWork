const joinGroupService = (groupId,userId)=>{ 
    // This function will add the user to the group
    // it will add the user to the group_participants table
    return
};

module.exports =  joinGroupService;