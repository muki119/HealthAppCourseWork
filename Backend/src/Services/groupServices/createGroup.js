const createGroupService = (groupId,userId)=>{
    // This function will create a group
    // It will return a group object
    // It will also add the user to the group
    // It will also add the group to the user's groups
    // will make a new group_participants entry
    // will make a new group entry
    return
};

module.exports =  createGroupService;