const database = require('../../config/Database');
const {Goal} = require('../../models');

const createGoalService = async (userId) => {
    try {
        
    } catch (error) {
        throw error;
    }
}

module.exports = createGoalService;